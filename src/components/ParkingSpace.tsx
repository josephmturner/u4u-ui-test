import React from "react";
import styled from "styled-components";

const ParkingSpace = () => <StyledParkingSpace />;

const StyledParkingSpace = styled.div`
  position: relative;
  height: 100%;
  width: 100%;
`;

export default ParkingSpace;
